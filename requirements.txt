# Django Xiangqi PvE Requirements

# Core Framework
Django==5.0.2
djangorestframework==3.14.0
daphne
channels

# CORS (if needed for API)
django-cors-headers==4.3.1

# Environment variables
python-dotenv==1.0.1

# Static files serving (Vercel / Production)
whitenoise==6.6.0

# Database (PostgreSQL for production)
psycopg2-binary==2.9.9

# Production server
gunicorn==21.2.0

# Development tools
pytest==8.0.0
pytest-django==4.8.0
