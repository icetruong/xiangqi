# ── Stage 1: Build Flutter Web ──────────────────────────────────────────────
FROM ghcr.io/cirruslabs/flutter:stable AS builder

WORKDIR /app

# Copy toàn bộ project Flutter
COPY . .

# Lấy dependencies
RUN flutter pub get

# Build Flutter Web (release mode)
RUN flutter build web --release

# ── Stage 2: Serve bằng nginx ────────────────────────────────────────────────
FROM nginx:alpine

# Xoá config mặc định của nginx
RUN rm /etc/nginx/conf.d/default.conf

# Copy nginx config tuỳ chỉnh
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy output của Flutter build
COPY --from=builder /app/build/web /usr/share/nginx/html

# Railway map cổng động qua biến PORT — nginx mặc định lắng nghe 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
